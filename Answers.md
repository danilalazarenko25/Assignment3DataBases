1) Функцвя обов'язково повертає результат і її можна інтегрувати у звичайні sql-запити за допомогою оператора select. Процедура не використовує return для видачі результату, не може викликатися всередині select, запуск здійснюється через call.
2) Ні, тригер неможливо виконати вручну. Він розроблений виключно для автоматичного спрацьовування у відповідь на (insert, update, delete). Для його активації необхідно виконати відповідну дію з таблицею.
3) Плюси: це працює максимально швидко, тому що базі не треба ганяти дані туди-сюди по мережі до програми, це безпечно бо все лежить захищеним в одному місці. Мінуси: такий код дуже незручно тестувати, важко шукати в ньому помилки і складно вести нормальну історію змін через той же гіт.
